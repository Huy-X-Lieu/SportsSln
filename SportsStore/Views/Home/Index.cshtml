@model ProductsListViewModels

@foreach(var prod in @Model?.Products ?? Enumerable.Empty<Product>()){
    <div>
        <h3>@prod.Name</h3>
        @prod.Description
        <h4>@prod.Price.ToString("C")</h4>
    </div>

}

<div page-model="@Model?.PagingInfo" page-action="Index"></div>